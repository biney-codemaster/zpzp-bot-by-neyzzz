# Token bot (Discord Developer Portal)
DISCORD_TOKEN=

# Préfixe par défaut (modifiable avec +setprefix)
PREFIX=+

# Couleur embeds (blanc par défaut)
EMBED_COLOR=FFFFFF

# ID(s) propriétaire bot, séparés par des virgules
OWNER_IDS=

# Chemin SQLite (optionnel)
DB_PATH=./data/bot.db
